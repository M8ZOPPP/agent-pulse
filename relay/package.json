{
  "name": "agent-pulse-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "dependencies": {
    "firebase-admin": "^13.4.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check ../hooks/agent-pulse-hook.mjs && node --check ../install.mjs"
  }
}
